# Bug2-Guided-Autonomous-Navigation
An autonomous mobile robot uses the Bug2 algorithm to navigate from a known start to a target while avoiding static obstacles. At the goal, an ArUco-based vision system detects and decodes markers, then compares their IDs with a predefined database to verify the target.
